---
description: Elovate backend coding conventions for agents
globs: backend/**/*
alwaysApply: false
---

# Backend conventions

When working under `backend/`:

- The API **connects to Postgres** at startup (`PostgresService`) using the repo-root `.env`. Feature routes are implemented; Swagger (`/docs`) documents them. Remaining work is JWT on `AuthGuard` and `POST /api/auth/sync-profile`.
- Check **permission codes** from the DB (`permissions.permission_code` via `user_roles` → `role_permissions`), never role name strings. The seed lives in `migrations/V2__identity_and_access.sql` — do not duplicate it in TypeScript.
- Signup always grants **learner**. Extra roles are additive (union of permission codes). Unassign extras with `POST /rbac/assignments/remove`; never remove `learner` or delete seeded roles.
- Guarded routes require a JWT (not wired yet). Permission codes are loaded from the DB (`AuthContextService`), never bypassed. Deactivated users and users in a suspended organisation do not load as `AuthUser`.
- Never hard-delete users, organisations, courses, or questions. Use activate/deactivate (orgs: suspend/activate) so history stays. `*.delete` permission codes mean deactivate, not HTTP DELETE.
- Use descriptive names (`organizationId`, `courseVisibilityId`, `oauthProviderId`, `emailAddress`). Match Flyway column names in comments and DTOs (`course_visibility_id`, not a made-up `visibility` column).
- Prefer `undefined` over `null` for absent DTO fields and optional query filters. Do not mix `null` and `undefined`.
- Do not coerce with casts (`as SomeType`, `Boolean(x)`, `!!value`, `Number(...)`, `String(...)`). Use explicit checks (`value !== undefined`).
- Group by type: `controllers/`, `services/`, `repositories/`, `guards/`, `helpers/`. Keep one controller per area. SQL lives in repositories (`users.repository.ts`, `courses.repository.ts`, `questions.repository.ts`, …). Services check permission codes and orchestrate.
- Every new/changed route needs `@ApiOperation` with: what table it uses, the permission code, and which roles grant it.
- Load env from the **repo-root** `.env` (see `src/helpers/env.ts`). Do not add a second secrets file under `backend/`.
- Connect to Postgres with `DATABASE_URL`, or with `FLYWAY_URL` + `FLYWAY_USER` + `FLYWAY_PASSWORD` (`PostgresService`). Schema changes belong in `/migrations` (Flyway), not TypeORM `synchronize`.
- Do not add TypeORM entities, extra DBML, or bootstrap scripts unless asked. Do not commit `.env`, `node_modules/`, or `dist/`.
- Also respect `backend/AGENTS.md`.
